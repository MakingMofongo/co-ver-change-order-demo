# Server-side only. Used by the /api/analyze route for LLM extraction.
# Never commit a real key. On Vercel, set this in the project's Environment
# Variables (it is read by the server, never exposed to the browser).
ANTHROPIC_API_KEY=
